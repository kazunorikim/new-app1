<%= form_with(model: apple, local: true) do |form| %>
  <% if apple.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apple.errors.count, "error") %> prohibited this apple from being saved:</h2>

      <ul>
      <% apple.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pine %>
    <%= form.text_field :pine, id: :apple_pine %>
  </div>

  <div class="field">
    <%= form.label :grape %>
    <%= form.text_area :grape, id: :apple_grape %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field :weight, id: :apple_weight %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
